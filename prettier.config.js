module.exports =
    {
        tabWidth: 4,
        useTabs: false,
        printWidth:
            -1, // Increase to avoid wrapping
        bracketSameLine: true, // Optional: Keep closing brackets on same line where possible
        singleQuote: true, // Optional: Consistent with your example
    };
